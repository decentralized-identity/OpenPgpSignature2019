const signedWithDomain = require("./signedWithDomain.json");
const signedWithProof = require("./signedWithProof.json");
const signedCompact = require("./signedCompact.json");

module.exports = {
  signedWithDomain,
  signedWithProof,
  signedCompact
};
